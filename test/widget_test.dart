// Legacy template test removed from app; keep the suite green by skipping.
import 'package:flutter_test/flutter_test.dart';

void main() {
  // Skipped placeholder: the default counter test doesn't apply to this app.
  testWidgets(
    'Legacy counter template (skipped)',
    (tester) async {},
    skip: true,
  );
}
